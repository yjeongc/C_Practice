/*#include <stdio.h>

//변수 선언
int a, b;

//메인 부분
int main(void) {
	printf("두 수를 입력하세요.");
	scanf("%d %d", &a, &b);

	for (int i = a+1; i < b+1; i++) {
		a += i;
	}
	printf("두 수의 합: %d", a);
	return 0;
}
*/